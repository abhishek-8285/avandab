# Avandab Roadmap (2026-09-07)

> Survey of built state vs specs, phased forward plan. Sources: `docs/01–08`, `AVANDAB_ZERO_COST_ARCHITECTURE.md`, `tech-specs/00-migration-ownership-index.md`, `tech-specs/fleet-registry-sop-parity.md`, `AGENTS.md` critical path, `git log`, `FAILURE_ANALYSIS_2026-08-31.md`. `docs/*.md` supersede on conflict.

## Where we are (built)

**Stack** (`docs/01`): pure-Go binary, Chi router, SQLite WAL (`modernc.org/sqlite`), goose migrations, server-rendered HTML (`html/template`, HTMX islands on 13/162 templates, hand-rolled SSE/fetch for live surfaces; Datastar JS vendored but unloaded) + live map (OSM/Google gl=IN tiles, config-driven), in-memory event bus + transactional outbox, TCP `:5023` GPS ingest, MQTT `:1883`, Expo SDK 52 mobile app.

**Migrations:** `db/migrations/` = 143 files, head `00149_identity_sequence_resync.sql`; `db/migrations_pg/` mirrors at 143. Next free slot **`00150`**. *(2026-09-11: verified on disk; index header matches.)* `00068–00072` confirmed ABSENT (RESERVED-UNBUILT still true).

**Recent direction:** tenant fail-closed hardening + `DefaultTenant` fallback removal (`e74c040`); vehicle compliance hard-block restore (`e9c23ad`); founder-signal tenant scoping; PG dual-engine parity + CI gate; per-tenant company profiles (`00125`); self-registration → `org_admin`.

**Built 2026-09-09 → 10 (28 commits):** telemetry hardening round 2 — per-tenant plates (`00135`), positions NULL backfill (`00136`), ack-after-accept + worker failure metrics, etag-304/spatial-bbox/delta sync on live endpoint, SSE keep-alive, cross-tenant sync-spoof fix, egress origin shield + static cache; AV teleoperation deck + `vehicle_commands`/`av_operator` (`00137`); scale indexes (`00138/00139`); S3/R2 storage driver + `files.tenant_id` scoping (`00140`, closes cross-tenant file-read hole); Cloudflare Turnstile bot protection; telemetry partition retention cleaner; PWA service worker + offline precache + client image compression; cross-compiled VPS deploy (`scripts/deploy-vps.sh`, systemd, rsync; compile-on-target blocked); Google gl=IN live-map tiles + India-bounded viewports; SOP trip-close odometer (B1 seed).

**Built per migration (spot-verified):** telemetry GT06/AIS-140 + provider parity (`00117`); trip state machine + dwell + detention; route/ETA jobs (`00066/00067`); GST engine + PDF/QR + settlement ledger + Razorpay; tenancy registry + trigger hardening (`00102–00105`); churn (`00073–00076`); files (`00077`); RAG RBAC (`00078`); leader leases (`00079`); driver lifecycle (`00108`); dispatch offers (`00109`); quotes (`00110`); settlement ledger (`00111`); multi-leg + multistop EWB (`00112/00113`); entitlements + subscription webhooks (`00114/00115`); push tokens (`00116`); comm outbox (`00118`); email pool (`00120`); booking/trip indexes + idempotency (`00121/00122`); work orders (`00123/00124`); fleet SOP parity (`00126` JSON API + SAP-tab HTML); EWB delivered lifecycle (`00127/00128`); fuel issues (`00142`); maintenance plans (`00143`); gate register (`00144`); facilities (`00145`); STO/load board (`00146`); fuel cards (`00147`); ESG (`00148`); identity resync (`00149`). Agent orchestrator + RL loop + approval gate. OpenAPI covers 185 paths (B12 closed, A6 re-audit green).

**Zero-code-presence follow-ups (repo-wide grep 2026-09-10, resolved 2026-09-11):** `ZMOTM_MR` report set, `IP41`/`IW28`, backhaul, STO portal / load board, ESG, fuel cards — all built since (B2–B11 + reports/plans APIs). KMPL partial (template + fuel audit exist; SOP report set not built). Mobile `.ts`-vs-`.tsx` question resolved by A10 inventory (13 wired, 4 stub mockups).

**Reality divergences (`AVANDAB_ZERO_COST_ARCHITECTURE.md` vs observed):** *(2026-09-10, C5 — reconciled in place: §2/§4B/§6 rewritten to observed reality, honesty banner added.)* Residual: §3 live-map caveat added (some public share/playback templates embed unofficial Google `vt` tile URLs — ToS-gray, not an official free API); `ALL_TECH_SPECS.txt` remains a pointer stub by design; ownership index header now correct (head `00149`, repaired 2026-09-08 and 2026-09-11).

## Phase A — Stabilize (ship in AGENTS.md critical-path order)

- **A1. Secrets + mock-honesty audit** (`docs/08` §5). Done 2026-09-11: fail-closed startup on known defaults already in `cmd/server/main.go:256` (pinned by `TestUsingKnownDefaultSecret`); mock carpet completed (`EXT-MOCK-` external IDs, `mockWarn` on all 3 GSTN e-invoice mock paths, pinned by `TestMockHonesty_SyntheticIDsCarryMockPrefix`; IRN format-locked exception documented); empty device pepper warns in non-dev (rotation = reprovisioning, so warn-only) + `default-pepper` doc corrected to empty-unset. No migration.
- **A2. Tenant-hardening tail sweep** (`docs/06` §1). Re-run tenant lint; wire `scripts/tenant-lint.sh` into CI. No migration. *(2026-09-07: lint at 0 warnings.)*
- **A3. FK-health triage (61 pre-existing violations)** (SOP spec §10 notes). Read-only enumerate + disposition per table. No migration. *(Done 2026-09-08: `foreign_key_check` 0 rows on dev copy at v128 and after migrating copy to head v134 — 125 REFERENCES clauses, so non-vacuous. Original 61 disposed by `scripts/cutover-data-cleanup.sql`: 9 role-id maps + 52 orphans.)*
- **A4. Ops auto-checks green** (`FAILURE_ANALYSIS` auto-checks). Cron backup + ensure scripts, DNS forwarder, opencode `HOME=/` guard, `/tmp` trap. *(2026-09-07: crontab set, hooks on.)* No migration.
- **A5. Migration-index doc repair** (index `:1-5,110`). Fix header (head `00128`), add missing `00112` row, reconcile `00121+ reserved` vs allocated. Docs-only. *(Done 2026-09-08.)*
- **A6. OpenAPI↔router parity audit.** Diff every `/api/v1/*` mount against `paths:`. Docs/tests-only. *(Done 2026-09-08: `docs/tech-specs/openapi-router-parity-a6.md` — 128 router vs 38 spec paths, 0 dead spec entries, 90-path B12 backlog grouped by domain. Rule: new routes ship with spec entries.)*
- **A7. PG-parity tail proof.** Done 2026-09-11: fresh PG16 `goose up` failed at 00137 (`roles_pkey`) — explicit-id seeds (00027/00064) desync identity sequences, silently dropping 00073's `customer` row. Fixed append-only: `00149_identity_sequence_resync` pair (PG backfill + resync, sqlite no-op marker) + two-phase startup (`UpTo(72)` → resync → `Up`, mirrored in both pg TestMains). Fresh-chain `up` green to 149, `down`/`up` proven, pg_integration green, `sqlite2pg --check` (versions match, 0 dirty/orphans) + `--dry-run` (44/44 tables, 2346 rows, 0 quarantined) clean. Bonus find fixed: `datetime('now')` sqlite-ism in facilities update broke PG writes (now `CURRENT_TIMESTAMP`).
- **A8. Vehicle legacy-delegation ADR** (SOP spec §10 Deferred). One paragraph: keep split vs finish delegation. No behavior change. *(Done 2026-09-08: `docs/tech-specs/legacy-delegation-adr.md` — freeze + opportunistic delegation, `bookings.go` as template, aggregate wins on conflict. Scope widened to `handlers/` god-files + `agent/tools.go`.)*
- **A9. EWB delivered-lifecycle staging proof** (`00127/00128`). Done 2026-09-11: `TestA9_TripDeliveryOnMigratedChain` migrates the real chain to head, publishes `TripDeliveredEvent`, asserts bill → `delivered` + `DELIVERED` audit row (00127/00128 CHECKs exercised end to end; constraint up/down pinned by migration tests, handler path by P4 test). No migration.
- **A10. Mobile stub inventory** (`docs/04`). Done 2026-09-11: `docs/tech-specs/a10-mobile-stub-inventory.md` — 17 registered screens classified (4 stubs: OnboardingOverview/BookingSchedule/EarningsOverview mockup images + unregistered EarningsOverview; 13 wired with API binding per row). Every mobile-hit endpoint verified present in spec + on router. No migration.

## Phase B — Feature gaps (SOP non-goals; each ≤1 migration; next free `00150+`)

*(2026-09-10 re-slot: `00129`–`00134` were consumed by infra seams — GSTN/verify seam (`00130`), license NULL backfill (`00131`), vehicle expiry nullable (`00132`), `email_verified_at` (`00133`), `ts_unix` (`00134`) — so B1–B6 slots re-allocated to `00141`+.)*

- **B1. Trip Start/Close `ZMOTM_MMS` (pp.6-8).** Done 2026-09-11 (no migration — `completed_at` exists since 00026; `00141` slot already consumed): close-odometer + SOP close dialog (seed), breakdown→`vehicle_breakdown` ops-alert hook (tested), and driver-reported `closed_at` (RFC3339, optional, future-rejected) persisted as `CompletedAt` for offline closes; `CompleteTripRequest.closed_at` in spec. Feeds Gate Register.
- **B2. Fuel issue entry (p.9). `00142`.** Completed (`00142_fuel_issues.sql`): Fuel station OP/CL pump readings recorded, litres issued calculated, pump & fuel top-up measuring points updated, validity expiry enforced per TMS_SOP p.9; REST API mounted at `/api/v1/fuel-issues`.
- **B3. Maintenance plans `IP41` (pp.10-15). `00143`.** Completed (`00143_maintenance_plans_ip41.sql`): Single Cycle Plans with `annual_estimate` (`00126`) KM projection, call horizon percentage, and automatic Job Card (`work_orders`, `00123`) generation; REST API mounted at `/api/v1/maintenance/plans`.
- **B4. Maintenance notifications `IW28` + dispatch block (p.13).** Completed (no-DB, leverages `00123` `work_orders`): `in_progress`/`assigned` cards block `IsMaintenanceBlocked` and trip assignment; verified in `TestWorkOrders_IsMaintenanceBlocked_DispatchBlock`.
- **B5. Reports `ZMOTM_MR` Gate/Fuel/KMPL/Breakdown (pp.16-20). `00144`.** Completed (`00144_trip_start_odometer_and_gate_register.sql`): Exact p.18 Vehicle-Master columns parity (`Vehicle No`, `Type`, `Category`, `Manufacturer`, `Model`, `Purchase Date`, `Purchase Value`, `Currency`, `Fuel Type`, `Fleet Number`, `Chassis No`, `Engine SNo`), Gate Register with depot in/out & net distance KM calculation, Fuel & KMPL report reconciling `fuel_issues` (`00142`) odometer run, and Breakdown/Job Card notifications report (`00058`/`00123`). Both CSV exports and REST APIs mounted at `/api/v1/reports/*`.
- **B6. Facility master / ZFID sync (p.1-2). `00145`.** Completed (`00145_facilities_master.sql`): `facilities` master table with p.2 screenshot columns (`facility_code` e.g. `MM21000000757`, name, `facility_type`, plant, circle, profit/cost center, address, coordinates, validity dates, `is_active`), multi-tenant triggers, and REST API mounted at `/api/v1/facilities` with RBAC gating.
- **B7. `00068` Backhaul matching (no DB).** Completed (`docs/tech-specs/b7-spec-19-backhaul-matching.md`): Return-load suggestions on completing/delivered corridors. Haversine distance spatial filter ($R \le 50\text{ km}$, configurable up to $150\text{ km}$), homeward corridor alignment (dropoff closer to depot than pickup), vehicle payload/body-type constraints, blocked/maintenance vehicle exclusion guards (`00123`/`00126`), deterministic rankings, and mobile dispatch offer integration (`00109_dispatch_offers`). REST APIs mounted at `/api/v1/backhaul/matches` and `/api/v1/backhaul/offers`.
- **B8. `00146` STO portal + load board listings.** Completed (`00146_sto_and_load_board.sql`, Spec 19 §2, re-slotted from 00069 reserved slot): Stock Transfer Orders (`DRAFT` → `RELEASED` → `POSTED` → `ASSIGNED`), Load Board syndication (`load_board_listings`), carrier bidding (`load_board_bids`), fail-closed award transactions, and REST APIs mounted at `/api/v1/sto/*` and `/api/v1/loadboard/*` with RBAC gating.
- **B9. `00070` CX tracking timeline (no DB, uses `00044` share).** Completed (`docs/tech-specs/b9-spec-20-cx-customer-timeline.md`): Public milestone timeline on share links (`/share/{token}` & `/api/v1/share/{token}/timeline`). Dynamic milestone derivation taxonomy (`ORDER_CREATED`, `VEHICLE_DISPATCHED`, `STOP_ARRIVED`, `IN_TRANSIT_CHECKPOINT` for FASTag tolls & geofence events, `OUT_FOR_DELIVERY` with 5km proximity detection, and `DELIVERED_POD`), PII phone masking (`+91 98*** **321`), driver earnings/costs omission, interactive Leaflet polyline path rendering, and plain `setInterval` polling for step progression (no Datastar/HTMX — verified 2026-09-12 rescan).
- **B10. `00147` Fuel cards + accounting-sync extension.** Completed (`00147_fuel_cards_and_accounting_sync.sql`, Spec 20 §2, re-slotted from 00071 reserved slot): Fleet fuel card management (IOCL, BPCL, HPCL, Shell, FleetBank), SHA-256 card token hashing & masking, automated reconciliation loop with vehicle tank capacity pilferage guard (`>105%` $\to$ `FLAGGED_ANOMALY` + `ops_alerts`), kharcha cross-check ($\pm 24\text{h}$, $\pm ₹10$) with automatic verified expense generation, money ledger double-entry sync (`debit` fuel expense + `credit` card clearing) and `accounting_sync_log` audit entries. REST APIs mounted at `/api/v1/fuel-cards/*` with RBAC gating. *(One-page spec: `docs/tech-specs/b10-spec-20-fuel-card-accounting.md`)*
- **B11. `00148` ESG snapshots.** Completed (`00148_esg_snapshots.sql`, Spec 20 §3, re-slotted from 00072 reserved slot): Scope 1 direct & Scope 3 Cat 4 upstream transport carbon accounting (GHG Protocol + SEBI BRSR Principle 6), fuel-primary & tonne-km fallback calculation methodologies, zero-division safety guards, audited Trip Carbon Certificates (`/api/v1/esg/trips/{trip_id}`), periodic fleet carbon snapshots (`/api/v1/esg/snapshots`, `/api/v1/esg/snapshots/generate`), and BRSR Principle 6 core compliance reports in JSON & CSV (`/api/v1/esg/reports/brsr`). *(One-page spec: `docs/tech-specs/b11-spec-20-esg-snapshots.md`)*
- **B12. OpenAPI gap closure.** Done 2026-09-11 in two rounds: `openapi.yaml` 38 → **183 paths / 214 ops** (round 1: `scratch/openapi_{b6_b11,drivers,ops_telemetry}.yaml`; round 2: `scratch/openapi_residual_{a,b,c}.yaml` covering control-tower/customer/founder/compliance/HSN, files/documents/accounting/payments-extras, B2 fuel-issues/B3 plans/B5 reports/experiments; all `$ref`s resolve). Deliberately out of spec: `/api/v1/integrations/*` (stub shapes till providers go real, per A6), `/api/*` non-v1 twins (agent/RAG/alerts/money-strip/fleet/search/driver-money/expenses-OCR/email-pool — not v1 surface). `POST /api/v1/outbox/batch` mounted in `cmd/server/main.go` (was documented-but-unmounted). No `/api/v1` routes exist for churn/entitlements/subscriptions/dispatch/quotes (services only, grep-verified). *(B7–B11 one-page specs delivered in `docs/tech-specs/`.)*

## Phase C — Future bets

- **C1. Multi-instance safety.** Built: `worker_leases` (`00079`) + leader election (all crons/sweepers run leadered; outbox relay CAS-claims). Done 2026-09-11: dwell duplicate fencing — zone-event ids were random UUIDs per persist attempt (retry → duplicate events/detentions/breach alerts); now deterministic `dwell-<sha>` ids + `ON CONFLICT DO NOTHING` insert-skip with side-effect skip, pinned by `TestDwellWorker_RetrySameEventsInsertsOnce`. Remaining: ingest/API topology split, lease tuning.
- **C2. PG cutover.** Done 2026-09-11: `docs/tech-specs/c2-pg-cutover-runbook.md` (freeze→migrate→verify→flip→rollback) rehearsed end to end on scratch PG16 — sqlite boot/migrate, freeze, backup-API copy, empty-PG boot to v149, `--check` clean, live 44/44 tables 2332 rows 0 quarantined, PG smokes (`/login`+token+trips 200), rollback smokes green. `pg-cutover.sh` backup step fixed (WAL-unsafe `cp` → backup API + freeze note). Timescale deferred to >5k-truck trigger; archival via partition retention cleaner. **SQLite exit trigger (decided 2026-09-11, calm conditions): cut over when ANY fires — transport.db >4GB, first multi-region serving need, or sustained WAL write contention (busy-timeouts in logs). Until then SQLite stays; no premature migration work.**
- **C3. Commercialization.** Built: catalog/subscriptions/meters (`00114`), sub webhooks (`00115`), profiles (`00125`), flags (`00089`), quota enforcement wired (gate + metering on booking create/complete/cancel). Done 2026-09-11: all three Razorpay webhooks fail closed without `RAZORPAY_WEBHOOK_SECRET` (subscription + payout were forgeable; payment already was) — pinned by 503 tests; secret documented in runbook. Done 2026-09-11: dunning progression sweep (`SweepDunning`: stale PAST_DUE → GRACE after 7d, GRACE → READ_ONLY after 14d, one step per run, leadered daily cron) pinned by `TestSweepDunning_ProgressesStaleDelinquencies`; live pricing (`GET /api/v1/plans` + admin `PUT /api/v1/plans/{id}`, pinned by `TestLivePricing_ListAndUpdate`, spec'd). Mutating agent tools stay approval-gated.
- **C4. Live provider integrations.** Done 2026-09-11: `docs/tech-specs/c4-provider-golive-runbook.md` (env, flip, exercise routes, rollback per provider). Verified without creds: live HTTP paths contract-pinned (`TestLiveContract_*`: path + `X-API-Key` + `*_unavailable` taxonomy); mock carpet extended to provider adapters (`<PROV>-MOCK-`, pinned). Honest boundary: accounting adapters are mock-only (`ErrNotImplemented` live) — implementing a real adapter is the remaining per-provider work, mock stays default till then.
- **C5. Zero-cost doc reconciliation.** Rewrite or supersede `AVANDAB_ZERO_COST_ARCHITECTURE.md` §§2/4/6 (device reality, mail path, PG path, OSRM posture).
- **C6. Network-effects bets (post-B7–B11).** Load-board liquidity, STO self-serve, CX timeline as wedge, ESG tenders, fuel-card float.

## Risks & open questions

1. Single-node fragility (2026-08-31 load-28/502 on 5.6 GiB device) vs 5k-truck claims — C1/C2 hedge; cutover rehearsed 2026-09-11 but not executed, still single-node — no scale promises.
2. 61 pre-existing FK violations — *(resolved: A3 done 2026-09-08, `foreign_key_check` 0 rows at v134+; dispositions in `scripts/cutover-data-cleanup.sql`)*.
3. Mock-by-default providers safe only while flags stay true; guard staging mocks from prod compliance.
4. Index drift invites number collisions (past `00081/00084/00085`) — do A5 before any B-ticket.
5. B7–B11 blocked on one-page specs each — *(resolved 2026-09-11: specs created in `docs/tech-specs/b7..b11`)*.
6. B6 soft-depends B1 Gate Register — *(moot 2026-09-11: both done)*.
7. OpenAPI survey was partial (through `/telemetry/*`) — *(resolved 2026-09-11: A6 re-audit green, 183 paths)*.
8. Mobile `.ts` screens may be stubs — *(resolved 2026-09-11: A10 inventory, 13 wired / 4 stubs)*.
9. Agent mutating tools stay gated — auto-billing must never bypass `/agent-actions`.
