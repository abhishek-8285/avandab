-- PG port of 00055_trip_pod_hardening.sql | status: PORTABLE | flags: none
-- +goose Up
-- 00055: Trip POD hardening (pod_otp_hash, converter/aggregate fields) — Spec 09
-- Placeholder: consolidated; no-op to maintain sequence.
SELECT 1;
-- +goose Down
SELECT 1;
