-- PG port of 00063_rag_vectors.sql | status: PORTABLE | flags: none
-- +goose Up
-- 00063: RAG multi-tenant vectors + provider registry — Spec 10
-- Placeholder: implemented outside goose (rag_vectors.db); no-op to maintain sequence.
SELECT 1;
-- +goose Down
SELECT 1;
